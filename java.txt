1. what is java. ?
 java is high level program lanavege ,object oriented program lanvege ,platfrom indenpended lanvege
 which is main used in back end codeing .
 
 
 2. explain how java file will execute .?
 
java execute in 2 stages 
1.sourcre to byte codeing
2. byte to machine code 
the sourcre code will be compled by java to byte code .it will be converted by compyler
the byte code to machine code the the jvm will work.


3. what is data type explain type of data type.?
 data type is a says what type of data or information will be stored in the variable.
 
 there are 2 type of data types
 1.permitve data type
 2.non permitve data type
 
 1. permitve data type
  the permitve data type are user defined data type 
  which will be defined by the user need the user can change the code or variable in the data type
  
  there are 7 or 8 data in permitve 
  1. int
  2.float
  3.char
  4 string
  5.bloalen
  6.long
  7.double
  
 2.non permitve data type
the non permitve data type will be the main java will be declured the values
it can not be changed by the user 

1.array 
2.tree
3.graph
4.list
5.static
6.string


4.what is variable explain types of variable ?

the variable are the continer to store the values in the program.
 the types of variables are 
 1. local 
 2.instances
 3.static
 4.parmertersed variable

1.local variable 
the local variable are the variable which can be the acessed inside a method 

2.instances variable
the instances variable are the can be access by the out side the method in side the class

3.static 
it is decluarde inside the class outside the method .

4.parmetersed variable
which canbe declured in inside  the method useing "()".


5.what is array?
 array collection of homegenes elements , which can be can the same date in a single arrary from ,
 the mulip values can be samed in the one array
 data or information shoble be same .
 

6.what is method explain types method ?

method is block of code perfroms task.

types of methods 
1.if
 if is a block of statments 

2.if else
if else method which works ,the statments will work on true or flase 

3.if else larder




7.what is constructor?

constructor is used to create object in class ,
inslluion  the obijcet  in class 







